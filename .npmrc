# 项目级 npm 配置，团队所有人 clone + npm install 走相同规则
#
# legacy-peer-deps: echarts-gl@2.0.9 的 peer 声明卡在 echarts ^5.1.2，
# 但实测与 echarts 6 运行时完全兼容（vite build / globe 渲染都正常）。
# 等 echarts-gl 上游放出支持 echarts 6 的版本后可删除此行。
# 详见 https://github.com/ecomfe/echarts-gl/issues
legacy-peer-deps=true
